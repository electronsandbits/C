/*
 * File: sum.c
 * -----------
 * This program computes the sum of two integers.
 */

#include <stdio.h>

int main(void)
{
   int x, y, sum;
 
   x = 12;
   y = 40;
   sum = x + y;
   printf("x = %d y = %d sum = %d\n", x, y, sum);

   return 0;
}
