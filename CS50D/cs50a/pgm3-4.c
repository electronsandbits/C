#include <stdio.h>
int main() {

   int count;

   count = 0;
   printf("\nThe initial value of count is %d.", count);
   count++;
   printf("\n count is now %d.", count);
   count++;
   printf("\n count is now %d.", count);
   count++;
   printf("\n count is now %d.", count);
   count++;
   printf("\n count is now %d.\n", count);

   return 0;
}
