#include <stdio.h>
int main() { */

   int length, width, area;

   width = 15;
   length = 20;

   area = length * width;
   printf("The area is %d\n", area);

   return 0;
}


#include <stdio.h>
int main () {
   
   int length, width, area;
   
   length = 20;
   width = 15; 
  
   area = length * width;
   printf("The area is %d\n", area);

   return 0;

}


#include <stdio.h>
int main () {

   int length = 20, width = 25, area;

   area = length * width;
   printf("The area is %d\n");

   return 0;
}
